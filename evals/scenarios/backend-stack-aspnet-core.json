{
  "id": "backend-stack-aspnet-core",
  "category": "backend",
  "title": "Arquivar nota em ASP.NET Core",
  "fixture": "aspnet-app",
  "prompt": "Adicione o endpoint POST /notes/{id}/archive (ASP.NET Core) que arquiva uma nota do usuário autenticado: deve ser idempotente, responder 404 para a nota de outro usuário e incluir a mudança de esquema necessária (coluna archived_at, nula) como uma nova migração, sem editar a migração inicial. Siga as convenções do projeto e as skills do kit. Não é preciso rodar o app nem instalar dependências. Termine com um resumo curto.",
  "timeoutSec": 900,
  "expected": [
    {
      "type": "tool_called",
      "tool": "reference/aspnet-core"
    },
    {
      "type": "tool_called",
      "tool": "skill/api-design"
    },
    {
      "type": "file_matches",
      "glob": "NotesApi/Migrations/*.cs",
      "pattern": "ArchivedAt"
    },
    {
      "type": "file_matches",
      "glob": "NotesApi/**/*.cs",
      "pattern": "MapPost\\([^)]*archive"
    },
    {
      "type": "file_matches",
      "glob": "NotesApi/**/*.cs",
      "pattern": "archive[\\s\\S]{0,700}UserId|UserId[\\s\\S]{0,300}archive"
    }
  ],
  "forbidden": [
    {
      "type": "file_matches",
      "glob": "NotesApi/Migrations/*CreateNotes.cs",
      "pattern": "ArchivedAt",
      "description": "editou a migração inicial"
    },
    {
      "type": "file_matches",
      "glob": "NotesApi/**/*.cs",
      "pattern": "\\.Notes\\.FindAsync\\((?![\\s\\S]{0,300}UserId)",
      "description": "busca sem escopo do dono: qualquer usuário alcançaria a nota de outro"
    }
  ]
}
