{
  "id": "backend-stack-express",
  "category": "backend",
  "title": "Arquivar nota em Express",
  "fixture": "express-app",
  "prompt": "Adicione o endpoint POST /notes/{id}/archive (Express) que arquiva uma nota do usuário autenticado: deve ser idempotente, responder 404 para a nota de outro usuário e incluir a mudança de esquema necessária (coluna archived_at, nula) como uma nova migração, sem editar a migração inicial. Siga as convenções do projeto e as skills do kit. Não é preciso rodar o app nem instalar dependências. Termine com um resumo curto.",
  "timeoutSec": 900,
  "expected": [
    {
      "type": "tool_called",
      "tool": "reference/express"
    },
    {
      "type": "tool_called",
      "tool": "skill/api-design"
    },
    {
      "type": "file_matches",
      "glob": "src/routes/*.ts",
      "pattern": "\\.post\\(\\s*['\\\"][^'\\\"]*archive"
    },
    {
      "type": "file_matches",
      "glob": "src/routes/*.ts",
      "pattern": "archive[\\s\\S]{0,500}(user\\.id|userId)"
    },
    {
      "type": "file_matches",
      "glob": "src/db/migrations/*.sql",
      "pattern": "archived_at"
    }
  ],
  "forbidden": [
    {
      "type": "file_matches",
      "glob": "src/**/*.ts",
      "pattern": "findById\\(req\\.params",
      "description": "busca sem escopo do dono: qualquer usuário alcançaria a nota de outro"
    },
    {
      "type": "file_matches",
      "glob": "src/db/migrations/001_create_notes.sql",
      "pattern": "archived_at",
      "description": "editou a migração inicial"
    }
  ]
}
