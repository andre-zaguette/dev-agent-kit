{
  "id": "backend-stack-rails",
  "category": "backend",
  "title": "Arquivar nota em Rails",
  "fixture": "rails-app",
  "prompt": "Adicione o endpoint POST /notes/{id}/archive (Rails) que arquiva uma nota do usuário autenticado: deve ser idempotente, responder 404 para a nota de outro usuário e incluir a mudança de esquema necessária (coluna archived_at, nula) como uma nova migração, sem editar a migração inicial. Siga as convenções do projeto e as skills do kit. Não é preciso rodar o app nem instalar dependências. Termine com um resumo curto.",
  "timeoutSec": 900,
  "expected": [
    {
      "type": "tool_called",
      "tool": "reference/rails"
    },
    {
      "type": "tool_called",
      "tool": "skill/api-design"
    },
    {
      "type": "file_matches",
      "glob": "config/routes.rb",
      "pattern": "archive"
    },
    {
      "type": "file_matches",
      "glob": "app/controllers/*.rb",
      "pattern": "def archive[\\s\\S]{0,400}current_user"
    },
    {
      "type": "file_matches",
      "glob": "db/migrate/*.rb",
      "pattern": "archived_at"
    }
  ],
  "forbidden": [
    {
      "type": "file_matches",
      "glob": "app/**/*.rb",
      "pattern": "Note\\.find\\(params",
      "description": "busca sem escopo do dono: qualquer usuário alcançaria a nota de outro"
    },
    {
      "type": "file_matches",
      "glob": "db/migrate/*create_notes*.rb",
      "pattern": "archived_at",
      "description": "editou a migração inicial"
    }
  ]
}
